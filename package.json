{
  "name": "plot-lines-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "test": "vitest"
  },
  "devDependencies": {
    "prettier": "3.2.5",
    "turbo": "^2.5.8"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "jsdom": "^27.0.0",
    "vitest": "^3.2.4"
  }
}
